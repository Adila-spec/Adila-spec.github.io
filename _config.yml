import { useState } from "react";
import { motion } from "framer-motion";

export default function Portfolio() {
  const [hovered, setHovered] = useState(null);

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-100 via-pink-100 to-green-100 text-gray-800 font-sans overflow-hidden">
      {/* Floating blob */}
      <motion.div
        className="absolute top-20 left-10 w-72 h-72 bg-pink-300 rounded-full blur-3xl opacity-40"
        animate={{ y: [0, 20, 0], x: [0, 10, 0] }}
        transition={{ repeat: Infinity, duration: 6 }}
      />

      {/* Hero */}
      <div className="relative z-10 text-center pt-24 px-6">
        <h1 className="text-5xl md:text-6xl font-serif text-purple-900">
          Adila ✦
        </h1>
        <p className="mt-4 text-lg font-sans text-gray-700">
          Frontend Developer • Designer • Creator
        </p>

        <div className="mt-8 flex justify-center gap-4">
          <button className="px-6 py-3 rounded-full bg-pink-200 hover:bg-pink-300 shadow-md transition">
            Projects
          </button>
          <button className="px-6 py-3 rounded-full bg-green-200 hover:bg-green-300 shadow-md transition">
            Contact
          </button>
        </div>
      </div>

      {/* Sections */}
      <div className="relative z-10 max-w-4xl mx-auto mt-20 px-6 space-y-12">
        <section className="bg-white/70 backdrop-blur-md p-6 rounded-2xl shadow">
          <h2 className="text-2xl font-serif text-purple-800">About Me</h2>
          <p className="mt-2 text-gray-700">
            I build soft, elegant, and modern web experiences with a focus on design, usability, and creativity.
          </p>
        </section>

        <section className="bg-white/70 backdrop-blur-md p-6 rounded-2xl shadow">
          <h2 className="text-2xl font-serif text-purple-800">Projects</h2>
          <ul className="mt-2 space-y-2">
            <li className="p-3 rounded-lg bg-pink-50">Portfolio Website</li>
            <li className="p-3 rounded-lg bg-green-50">SQL Practice Tracker</li>
            <li className="p-3 rounded-lg bg-purple-50">Python Interview Prep Tool</li>
          </ul>
        </section>

        <section className="bg-white/70 backdrop-blur-md p-6 rounded-2xl shadow">
          <h2 className="text-2xl font-serif text-purple-800">Coding Space</h2>
          <p className="text-gray-700 mt-2">
            SQL practice • Python questions • behavioral prep • project uploads
          </p>
          <code className="block mt-4 p-3 bg-gray-100 rounded-lg text-sm">
            SELECT * FROM skills WHERE level = 'growing';
          </code>
        </section>
      </div>

      {/* Footer */}
      <div className="text-center mt-20 pb-10 text-gray-500 text-sm">
        Built with ✦ pastel energy
      </div>
    </div>
  );
}
